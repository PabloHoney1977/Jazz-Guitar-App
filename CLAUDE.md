# Jazz Guitar Lab — Project Context

## Workflow Preferences
- Always use model **claude-opus-4-8** (set in `.claude/settings.json`).
- **Checkpoint Protocol (auto-save memory):** The user works in short sessions and uses `/clear` often to save tokens, so nothing important may live only in the live chat. Therefore: **whenever a meaningful unit of work finishes** — a fix shipped, a decision made, a direction changed, a fact learned that a future session would need — immediately update the **`## Working Memory`** section below (and the relevant durable section, e.g. Pending/Next Session Priorities or Decided Against) **without being asked**, then end that turn with a one-line prompt to the user: `📝 Memory checkpointed: <what was saved>`. This is the "auto-fire on finishing something + you get a prompt" behavior. Keep Working Memory short (a rolling snapshot, not a log) — overwrite stale lines, don't endlessly append. Don't checkpoint trivial Q&A or read-only answers; only checkpoint things that would hurt to lose on `/clear`.
- When making a fix, always commit it without being asked — only skip committing if explicitly told not to.
- After committing, always push to the appropriate branch immediately — do not wait to be asked.
- **Memory edits always land on `main`.** Any change to this file (or any other memory/context file, in any repo) must be merged to `main` and pushed — never left sitting on a feature branch. The user reads memory from `main`, so a checkpoint parked on a branch is invisible and may as well not have been written. This applies even when the code change it accompanies is still on a branch.
- The app is served from `main`; always ensure finished work lands on `main` and is pushed. Merge finished fixes into `main` automatically without asking each time (commit on a feature branch, then merge to `main` and push).
- **Git gotcha (remote-env quirk):** the fresh container clones a stray local `main` with unrelated history (just "Initial commit" / "Improve readability") — do NOT base work on it. The real served main is `origin/main`. A plain `git merge` of a feature branch into local main fails with "refusing to merge unrelated histories". To land work on main: `git fetch origin main`, then `git checkout -B main origin/main` (or `git reset --hard origin/main`), then bring in the fix (`git cherry-pick <fix-commit>` applies cleanly), then `git push origin main`. Other sessions push to `main` concurrently, so `git fetch origin main` again right before pushing and expect to merge `origin/main` before `git push` succeeds.

### Git Workflow Notes (environment-specific)
- No build step — `main` is served live, so landing on `main` = instantly live for testing.
- **Rollback checkpoints use a branch, not a tag.** The session git proxy blocks tag pushes (HTTP 403) and there's no tag-creation API tool. Use a `baseline-YYYY-MM-DD` branch (e.g. via the GitHub `create_branch` API from `main`) as a named known-good restore point. Current baseline: `baseline-2026-06-26`.
- **To undo a live change, use `git revert <sha>`** (creates a new undo commit, safe to push). Never `reset --hard` on shared `main`. Keep commits small and atomic so reverts are surgical.

## Working Memory (live session checkpoint)
> Rolling snapshot of "where things stand right now" so a fresh session after `/clear` loses nothing. Updated automatically per the Checkpoint Protocol above. Keep it terse and current — overwrite, don't append a diary. Last updated: 2026-08-24.

- **✅ MONEY/TAX/AGREEMENTS CHAIN FULLY CLEARED — critical path is DONE (2026-08-24).** App Store Connect Business page confirms: **Capital One LLC checking (…3825, USD) Active**, **W-9 Active** (submitted 2026-08-22), **Paid Apps Agreement Active** (Jul 16 2026–Jun 26 2027) + Free Apps Active, **DSA Active** (27 countries). This **retires the "⛔ CRITICAL PATH IS THE BANK ACCOUNT" blocker below** — the bank is open, verified, and Apple can now pay the LLC directly. `pro_unlock` can be sold. **Remaining gaps before submit (all under the user's control now):** (1) ⚠️ **DSA trader address is still the home address** — change to the motel (708 Water St, Genoa WI) + Google Voice + support@ email before EU go-live, OR launch US-only and add EU later (it goes public the moment the app is EU-live). (2) **App Store listing assets** — iPad 13" screenshots (2064×2752, a hard block since iPad support is declared) + iPhone screenshots, description, age rating, **required privacy-policy URL**, IAP review screenshot (the paywall sheet). (3) **`support@highlandmusicapps.com` forwarding** was never actually set up — it's the address App Review writes to. (4) **Production purchase test** — buy `pro_unlock` with a real card on launch day (sandbox ≠ production; sandbox already passed on device). (5) **Final Codemagic→TestFlight build** off current `main` (new icon, audio rebuild, `?dev=1` gating), + `ITSAppUsesNonExemptEncryption=false` in Info.plist, + choose **"Manually release this version"** at submission. (6) **Small Business Program** — enroll now (never backdated; effective the 1st of the month *after* approval, so late enrollment pays 30% on the launch burst). PostHog key still a placeholder — fine to ship (no funnel data).

- **✅ PRO-LEAK AUDIT + web build closed (2026-08-22, commit f44cd82 on `main`).** Sandbox purchase **succeeded on device** — user is moving to launch. Full pass over every gate: **in-app gating is clean** (Play forms, vTypes in both Keys and Chords, ear-training tabs/tiers/harmonic/Auto, Any Chord types ≥4 + extensions, Find Chord, Favorites all route through `isEss`→`onUpgrade` and never mutate state; every Pro Guide preset carries `fullPreset:true`; `jg-form`/`jg-vtype` rehydrate Pro values from storage but the mount effects at `IIVIView` snap them back; `doUpgrade`/`doRestore` fail closed on native). **The leak was the web build, not the app:** repo is public with **GitHub Pages on**, and on web `doUpgrade` fell through to the dev unlock, `doRestore` granted unconditionally, the `Pro ✦` chip toggled tiers, and the trial re-armed on any site-data clear — i.e. the whole $14.99 product, free and installable to a home screen, one link away. **Fix:** dev unlock is now opt-in via **`?dev=1`** (sticky in `localStorage` as `jg-dev`) — keeps the fast Pro/Essentials test loop, leaves the public build fail-closed; web upgrade sheet now points at the iOS app and hides the trial button. Also removed two latent grant paths: `openPreset`'s `if(p.level) setLevel(p.level)` and the **unrendered `GuitarToggle`** (a one-tap essentials→pro switch sitting in the bundle). **3 new gating tests** guard all of it. `npm test` **47/47**, `npm run test:smoke` 96/96. **⚠️ Still open, user's call:** (a) no revocation on refund — `checkEntitlement` grants Pro but never takes it back, so a refunded buyer keeps Pro forever (deliberate one-way: downgrading on a false negative would strip a paying user); (b) trial is still re-armable by delete+reinstall (honor system, known); (c) repo privacy + Pages still not turned off.
- **🚀 LAUNCH PLAN REVISED 2026-08-25 — RELEASE SAT 8/29, BURST SUN 8/30 (two different days, on purpose).** Full day-by-day checklist artifact: **https://claude.ai/code/artifact/4fc3c953-dd38-42c3-a570-af3d6170df5a** (53 items, checkbox state persists in the user's browser). **The change:** the user asked whether Apple's trend algorithm rewards launching *on* the burst day. It does not — **ranking counts when the downloads land, not when the app went live**, so releasing early costs nothing on the charts. Releasing Saturday buys three things Sunday can't: (a) **store propagation takes ~1–4h**, occasionally longer, so a Sunday-morning Release + immediate messages sends friends to a dead link; (b) the **real-card production purchase test** and a **clean install on a device that's never had the app** can only happen once it's live, and are worthless discovered mid-burst; (c) a day of margin to fix whatever those find. **Shape now:** Tue 8/25 final Codemagic build + device pass · **Wed 8/26 HARD DEADLINE to submit** (unchanged — it is the only rejection margin) with **"Manually release this version"** · Thu 8/27–Fri 8/28 write the friend messages, send nothing, keep clear for a rejection · **Sat 8/29 Release + verify + real-card buy + put the link on the site, and send NOTHING** (every Saturday install is one missing from Sunday's window) · **Sun 8/30 morning US** fire everything in one window. **Expectation set honestly with the user:** 10–30 friend installs will NOT chart (Music/Education needs hundreds–thousands/day) — the burst's real payoff is **ratings + social proof**, which lift product-page conversion, which drives App Store *search* ranking over following weeks.
- **💵 SMALL BUSINESS PROGRAM SUBMITTED 2026-08-24 — and it is NOT a reason to delay launch (decided 2026-08-25).** User enrolled, then asked whether to push go-live back until it's approved. **No.** The key fact: **SBP's 15% rate takes effect the first day of the month after APPROVAL, not after launch** — so delaying the release does not pull the rate forward by even a day. Delaying only avoids 30% on whatever sells Aug 29–31, which at a realistic 5–20 friend purchases is **$11–$45 total**, against losing the coordinated weekend and the primed friend list. Downside case is worse: if approval slips into September, a September launch still pays 30% until Oct 1 — delaying could save literally nothing. Revenue shape agrees: the burst is mostly free downloads, paid conversions trickle in over months, so essentially all lifetime revenue lands post-Sept 1 at 15% regardless; those 3 days are a rounding error. **SBP status does not gate anything** — the Paid Apps Agreement (Active) is what makes `pro_unlock` sellable; an app launches and sells fine with SBP pending, and a new developer auto-qualifies (under $1M prior-year proceeds). Only open item: confirm the enrollment actually submitted rather than sitting as an incomplete draft.
- **📣 FORUM/REDDIT POSTING = WEEK 2, NOT LAUNCH DAY (decided 2026-08-25).** User asked about timing and worried trolls would retaliate with bad ratings. Three independent reasons all say wait until ~Sept 1+: (1) **early ratings are a one-time buffer** — with 30 honest ratings two hostile 1-stars barely move the average; with 3 ratings they wreck it, and a low average suppresses the downloads that would dilute it; (2) **Sunday is a weak Reddit day** anyway (engagement peaks weekday mornings, ~Tue–Thu 8–10am ET), so waiting costs no reach; (3) **r/jazzguitar and r/guitarlessons both restrict self-promotion** and automod filters accounts with no history in the sub — a removed post is a wasted shot. **On the troll fear:** direct retaliation is real but rarer than it feels (downvoting is free, installing an app to punish someone isn't). The likelier damage is **audience mismatch** — r/jazzguitar skews advanced, the app targets intermediate, so expect credible, specific 3-stars from people who expected more; that's an argument for how to frame the post, not for skipping. Rules for posting: lead with something useful that stands alone (the drop-2 string-set geometry), **say "I built this" in the first line** (concealment is punished harder than promotion), **never ask for ratings on a forum** (that's what flips a sub hostile), one sub at a time days apart, answer criticism without defending, and check for a sanctioned weekly self-promo/feedback thread first. **Apple will not remove an unfair rating** (only policy-violating ones), but you can publicly reply to every review from App Store Connect. Last resort only: App Store Connect can reset the ratings summary on a new version — it clears the good ones too.
- **✅ ~~THE CRITICAL PATH IS THE BANK ACCOUNT~~ — CLEARED 2026-08-24.** `pro_unlock` was blocked on the **Paid Applications Agreement going Active** (needs signed W-9 + verified LLC bank). All three are now **Active** on the Business page (Capital One …3825, W-9 submitted 8/22, Paid Apps Agreement Active). Name-comma risk also resolved (Apple corrected "Highland Music, LLC" → `Highland Music LLC` on 2026-08-20). The 8/30 launch date is no longer at risk from banking.
- **Newly surfaced gaps (2026-08-22), not previously tracked:** (a) **iPad screenshots (13", 2064×2752) hard-block submission** — iPad is the primary target so the app declares iPad support; (b) **`support@highlandmusicapps.com` was never actually set up** (Porkbun→Gmail forwarding still a to-do) and it's the address App Review writes to; (c) **Small Business Program is never backdated** — effective the 1st of the month *after* approval, so enrolling post-launch pays 30% on exactly the launch burst; (d) **"Manually release this version"** must be selected at submission or the app goes live whenever approved and wastes the coordinated install day; (e) add **`ITSAppUsesNonExemptEncryption=false`** to Info.plist to stop the per-build export-compliance prompt; (f) **buy the app with a real card on launch day** — sandbox ≠ production; (g) **DSA escape hatch:** if the trader address can't be changed in time, exclude the EU from availability at launch and expand later.
- **Ratings push (friends/trending):** ratings themselves have **no deadline**, but **charts run on ~24h download velocity** — concentrate installs into one window. **The in-app rating prompt fires at streak day 14, so nobody in the launch group will ever see it** — the direct ask + `https://apps.apple.com/app/id<APP_ID>?action=write-review` do all the work. Installs must come from the **App Store, not TestFlight** (TestFlight installs don't count and those users can't rate). Guideline 3.10: honest unincentivized asks are fine; **never** offer anything in exchange or supply suggested review text. Draft ask copy lives in the artifact.
- **✅ Header rebuilt around a real wordmark (2026-08-16, same branch).** User: the "Jazz Guitar Lab" title "looks kinda lame". Diagnosis: **the header wasn't laid out, it was wrapping** — one `flexWrap` row whose contents (156px title + 44px theme toggle + tier + streak + Overview/? Tour/···) exceeded the 334px of usable width, so the utility group fell to a second line. **88px tall, five elements at five different heights (20–44px)**, and a 44px utility button welded to the wordmark's elbow. **Fixes:** (1) New **`BrandMark`** component — inline SVG miniature of `icons/icon.svg` (nut, 4 strings, 3 frets, the four chord-tone dots in `TC`). The app already had a distinctive mark and the header rendered none of it; grid lines ride `--lbl` so it's theme-aware, dots keep their vivid hex. (2) **`···` is now the single help/settings menu** — page tour, overview tour and the theme toggle all moved into `AboutSheet` (new `onPageTour`/`theme`/`onToggleTheme` props). (3) Header is **one row, 88px → 44px**, everything on a shared centre axis. **⚠️ Watch the width budget:** an intermediate version measured `scrollWidth` **401px vs clientWidth 334** and silently clipped `···` off the edge — `html,body{overflow-x:hidden}` hides this, so **never trust `document.scrollWidth` to detect header overflow; compare the header's own `scrollWidth`/`clientWidth` and the last child's `right`.** Final budget verified at 375/390/430px with a three-digit (`🔥365d`) streak: `scrollW === clientW` everywhere. Wordmark drops below 380px (mark alone carries identity on SE); brand block is the only `flexShrink:1` element, `minWidth:27`. Long-press-for-audio-self-test preserved on the brand block. Smoke **Test 10 rewritten** — it looked for the theme toggle in the header, which is a real relocation, not a regression. `npm test` 44/44, `npm run test:smoke` **96/96**.
- **✅ Review tail-end cleared (2026-08-16, same branch).** (1) **Voicing explanation moved below the neck** in *both* Keys and Chords — `Guide tones: R + 3rd + 7th…`, the m7♭5 ♭5 warning, the Drop 2+4/2+3/rootless/arpeggio notes. They describe what the fretboard is drawing, so they now live in the neck panel footer via a new `vtypeNote` const; the controls bar above only renders when there is something to *operate* (`DROP_TYPES.has(vType)` → String set buttons), instead of existing purely to hold prose. (2) **Guide's duplicate "Start Here"** — the resume card's eyebrow and the collapsible intro had the same name side by side; intro renamed **"How this guide works"**, which matches its own subtitle. (3) **Ear Training intro** `paddingTop` 25vh → 8vh (heading was ~350px down under a 137px header; now 235px). **⚠️ (4) The `F#`-among-flats root spelling is NOT a bug — do not "fix" it.** `FLAT_KEYS={1,3,5,8,10}` deliberately excludes 6, so key 6 spells with sharps. Relabelling it `Gb` would require adding 6 to `FLAT_KEYS`, and `N_FLAT` has no `Cb` — Gb major's 4th would render as a wrong `B`. The mixed look is correct given the note tables. `npm test` 44/44, `npm run test:smoke` 95/95.
- **✅ Play tab decluttered + app-wide touch-target/type-size pass (2026-08-16, same branch).** Two remaining items from the full-app review. **(1) Pro's progression picker was worse than Essentials'** — 13 variable-width buttons free-wrapping into five ragged rows, **292px** of chooser, while Essentials got the tidy one-row shelf; the paying user had the messier screen. New `formShelf(label,forms)` helper renders `Progressions` and `Standards` as two horizontally-scrolling shelves with an edge fade (same pattern as the Essentials branch, which is untouched — smoke Test 23 still guards it). Form row **292px → 143px**; transport 465→**300px**; fretboard **742→592px, now above the fold**. **(2) The ★ Favorites button** was the worst control in the app: `marginLeft:'auto'` inside the wrapping row orphaned it onto its own line as a bare unlabelled star whose only explanation was a `title` tooltip (invisible on touch), and it silently `return`ed on a duplicate. Now a labelled `★ Save this` in a `Favorites` row that flips to `★ Saved` — new module-level `faveSaved`. **(3) Touch targets** below Apple's 44pt floor raised: `MIX ▾` (was **57×14** — a third of minimum, and the only way into the EQ panels), theme toggle (31×27), `TAP`, `▶ hear`, `▸ Why it works`, `In a key ↗`/`Explore ↗`, the ear-training mode row (30px), `Customize this bar` (16px tall). MIX/LED keep their compact variant while `isPlaying`, when the transport deliberately shrinks. **(4) Type under 11px** raised to 0.7rem: `MIX`/`CLICK` (**8.8px**), `BASS`/`GUITAR`/`RIDE` (9.9px), bottom-nav labels (10.2px), `m7b5` quality symbol (9.3px), `BPM`, the Dots toggle. Left alone deliberately: the `8fr`/`10fr` fret annotations inside `ChordBox` (10px) — enlarging them risks the two-digit clipping bug noted below. `npm test` 44/44, `npm run test:smoke` 95/95.
- **✅ Light-mode contrast pass — WCAG AA clean, now guarded by a test (2026-08-16, branch `claude/chords-interface-review-omeyoh`).** A full-app UI review measured **26 text elements below WCAG AA in light mode** (Keys 11, Play 11, Chords 4) vs **0 in dark** — the exact failure this file already warned about (bright pastels wash out as *text* on the light bg) had only ever been fixed for the Play-tab LEDs. Worst offenders: the chord-tone legend `7th` at **1.19:1** (yellow on near-white), and the **selected** voicing tab `Shell` at 1.65:1 — the selected item was the least readable thing on screen. **Fix is pure palette, zero layout change:** new `--tc-txt-0..3`, `--acc-{red,teal,blue,amber,green,purple,orange,warn}` and `--gold-txt` in both `:root` blocks; dark keeps the bright hexes, light uses darkened variants each measured ≥4.5:1 against `--bg2`. In `app.js`: `TCT`/`TCT_RL` arrays, the `A_*` consts, `GOLD_TXT`, plus **`accTxt(hex)`** for spots where one value feeds a border *and* a label. ~80 sites converted; **only `color:` changed — every dot fill, glow, border and background keeps its vivid hex** (verified: neck dots still `#FF6B6B`). Gold had to split in two: `--gold` (#b07418) stays the CTA-*background* value because near-black text on it needs the lighter gold (4.90:1), while `--gold-txt` (#8a5a10) is the text value — one variable could not serve both roles. **Dark mode is pixel-identical** (additions only). **New smoke Test 27** walks 5 tabs × 2 themes asserting every visible leaf label clears AA, so a hardcoded bright hex in a future `color:` fails CI. `npm test` 44/44, `npm run test:smoke` **95/95**.
- **✅ Chords-page (Any Chord) layout restructured — 2026-08-16, branch `claude/chords-interface-review-omeyoh`.** User asked for a skeptical review of the Chords page ("looks button-heavy"). Diagnosis: **not the button count** — 40 tap targets sat *above* the fretboard, so the neck started **782px down** on a 390×844 phone (measured, not guessed). The page read as a form when it should read as an instrument. Five fixes: (1) **Root + Chord type + Extension now live in a disclosure** whose collapsed header *is* the old chord info bar (name, quality, tones, "In a key ↗") — so collapsing hides controls, never information. **Collapsed by default** (`jg-chord-picker`, `'1'`=open); header pill reads `C7 change ▼`. Neck now starts at **406px, fully visible**. (2) **Scale overlay moved below the neck**, into the neck panel beside the Dots toggle — it only changes what the fretboard draws, so it shouldn't gate access to it. (3) **Root grid → fixed 6-wide, Chord type → 5-wide**, killing the 10+2 / 8+2 orphan rows. (4) **Voicing "tabs" → even grid cells** (`auto-fit minmax(92px,1fr)`) in *both* Chords and Keys — 6–7 variable-width tabs wrapped to a ragged second row and broke the join with the panel below; that controls bar is now a self-contained rounded panel. (5) **Page-tour custom step 1 retargeted** from `chord-type-tabs` → new `chord-picker` header: the old target doesn't exist while collapsed, and pointing at the disclosure is what actually needs teaching. `npm test` 44/44, `npm run test:smoke` 85/85, verified visually at 390×844 in both states.
- **🐛 The streak never expired (fixed, same branch).** `markPracticed` only recomputed the streak *when you practiced*, so a lapsed streak kept its stored value forever — which is exactly the contradictory banner the user screenshotted: **"⚠️ 1-day streak — 8 days since last practice."** New module-level **`liveStreak(raw,last)`** expires it unless the last practice was today, yesterday, or (grace day) two days ago from a streak ≥3 — mirroring the rule `markPracticed` already applied. Wired at the `streak` state initializer and persisted by the mount effect, so every consumer (header 🔥 badge, banner, milestones, Guide resume card) is corrected at the source. That effect now also **cancels the reminder for a dead streak** instead of nagging about a streak already lost. Banner copy split in two: alive → "🔥 N-day streak — practice today to keep it"; lapsed → "👋 Welcome back — N days away…", naming the lost streak via new `lapsedStreak` (captured once at mount).
- **✅ CORRECTED 2026-08-16 — the "`npm run sync` HARD GATE" was overstated.** The 2026-07-26 note said Package.swift registering zero plugins meant Purchases/LocalNotifications/InAppReview were no-ops on device. **That is not true for Codemagic builds:** `codemagic.yaml` runs `npm run build` **and `npx cap sync ios`** as build steps 2–3, so CI regenerates Package.swift from `package.json` on *every* build — the committed file never reached the compiler. Verified by actually running it: sync reported `Found 3 Capacitor plugins for ios` and wrote all three into Package.swift (now committed, ddcf82e, so the tracked file matches reality). Also repaired `package-lock.json`, which was **missing `@capacitor-community/in-app-review` entirely** despite it being a declared dep — an `npm ci` would have failed on that. **Still genuinely unverified (do NOT mark done):** a real **sandbox purchase test** on device, and confirmation that notifications actually fire. Those need a build + a human; the plugin-registration blocker itself is cleared.
- **Full usability/accuracy review done + 6 fixes landed on `main` (2026-07-26, commit d715bb0; feature branch `claude/opus-5-usability-review-uo9q8t` also pushed).** Fixed, in risk order: (1) **IAP fail-closed** — `doUpgrade`/`doRestore` no longer grant Pro free on a native build with no Purchases bridge; `AboutSheet` restore now reports its real outcome instead of always saying "restored ✓"; (2) **PERKS omitted Find Chord + Favorites** (both Pro-gated — the sheet sold features it never named); (3) **`FEATURE_DESC` exact-match misses** left the main Ear Training paywall, Jazz Standards, Favorites, All 12 intervals and `'Drop 3'` (vs `'Drop 3 voicings'`) with **zero value copy** — all filled; extended chord types now reuse `EXT_TYPES.ctx`; (4) **two factually wrong blurbs** — Stella said "(E♭, G, B♭) in 16 bars" (it's 32 bars, middle key **F**), Another You said "12-bar A section" (it's 16); (5) **♭5 mislabelled `#11`** on m7♭5/dim in `ScalePanel` — new `intName(interval,isTone)` helper (the neck was already correct: `getScalePos` excludes chord tones); (6) **`ChordBox` clipped two-digit fret labels** — "10fr" rendered at x=−1.2 on the *default* Play tab in C; `W` 128→134, `PL` 28→32 → x=+2.8. Also added a **Restore link** + **"One-time purchase · no subscription"** to the sheet, with the **action block made sticky** (the added copy had pushed the buy button 2px below the fold on 390×844 — measured, then fixed).
- **Review verdict for context:** musical content is the app's strongest asset — all 5 standards verified bar-by-bar against real changes (ATTYA's full 36 bars incl. G♭7–Cm7–B°7, Stella's full 32, Autumn Leaves' descending-fourths last A) and all correct. Weakness was never the music, it was the last mile: paywall undersold at the highest-intent moments, and the native plugin sync gates both revenue and retention.
- **✅ SMOKE SUITE IS NOW FULLY GREEN — 85/85 (`npm test` 44/44).** The 2 long-standing "known baseline" failures were **stale assertions**, not app bugs, and were rewritten (Test 23) to describe the locked-forms shelf the app actually ships. **There is no acceptable-red baseline any more — a failure now means a real regression.**

- **✅ APPLE ACCOUNT CONVERTED TO THE LLC + EIN FAXED (2026-07-15):** Two structural blockers moved. (1) **The Apple Developer account is now associated with the LLC (Organization) — Highland Music LLC is the seller.** The liability-shield goal is achieved on Apple's side; the website/domain-association requirement is fully cleared and closed. (2) **EIN: SS-4 faxed 2026-07-14** to 855-641-6935; **CP 575 arrives BY MAIL ~mid-Aug 2026** (return-fax box left blank on purpose — free send-only fax). The IRS **online tool 101s** (name conflict, won't self-clear) and **the phone loops back to online-only** (domestic phone EIN issuance is discontinued) — **fax is the only path; don't re-suggest either dead end.** Also discovered: the **exact legal name is `Highland Music LLC` — NO COMMA** (per the WI Articles); CLAUDE.md is corrected, but the **`highland-music-site` footer still says "Highland Music, LLC" and needs the comma removed**. **Next:** wait for CP 575 → open free LLC checking (Mercury/Relay/Bluevine, needs the EIN) → set App Store Connect banking + W-9 and RevenueCat payout to the LLC/EIN. **Decided: do NOT bridge via personal banking** (no revenue pre-launch, and personal bank/SSN under an LLC seller = entity mismatch → payment-hold risk). Full detail in memory file `ein-llc-banking.md`. **NOTE:** per the 2026-07-12 multi-project decision below, portfolio/business-level items like this belong in the `highland-music-site` ops-hub CLAUDE.md — mirrored here for now because the EIN/banking gate directly blocks this app's IAP payouts.
- **⚠️ DSA trader address = home address, needs fixing before EU launch (2026-07-16):** On the way to Paid Applications, Apple's Digital Services Act step forced a trader/non-trader declaration. LLC selling IAP = trader (not optional). The address field wouldn't edit inline, so it was completed with the **home address** — which would display **publicly** on the EU App Store page. **Not yet public** (only exposes once an app is EU-live), and editable before submission. Pre-launch fix: swap to the **motel at 708 Water St, Genoa WI** (real commercial address, keeps home private) + Google Voice phone + support@highlandmusicapps.com. Logged as a checklist item + memory `dsa-trader-address.md`. **✅ RESOLVED 2026-07-16:** accepted the Paid Apps Agreement terms (stage 1/3, free, no EIN) and **created the `pro_unlock` IAP** (Non-Consumable, status "Prepare for Submission", Product ID matches `app.js`) — so the **EIN wait does NOT block IAP setup**. EIN only gates the agreement going Active + the app going live/purchasable. **✅ RevenueCat fully wired 2026-07-16** (app + entitlement `pro` + `pro_unlock` attached + default offering Lifetime package + `appl_` key in app.js commit 7ee6460) — ready for a sandbox test after `npm run sync` + build; no EIN needed for sandbox. Remaining before submission: confirm IAP price $14.99 + add the required IAP review screenshot. Watch-item: Apple account legal name has the comma ("Highland Music, LLC") vs no-comma EIN — likely the culprit if the banking/W-9 name-match complains later.

- **Multi-project structure decided (2026-07-12):** User is close to publishing the first 3 apps (Jazz Guitar Lab, Piano Chords Lab, Note Quest) and asked how to organize folders/Claude memory across them. Decision: keep one repo + one `CLAUDE.md` per app (this file stays scoped to Jazz Guitar Lab only — don't let other apps' details creep in here). Portfolio-level/business concerns (LLC + Apple Individual→Organization conversion status, D-U-N-S, shared vendor decisions like RevenueCat/PostHog/Codemagic, cross-app technical gotchas) now live in a **separate ops hub**: `pablohoney1977/highland-music-site`'s `CLAUDE.md` (that repo already hosted the LLC's public site, so it does double duty rather than spinning up a new repo — a fresh `highland-music-ops` repo was attempted first but the GitHub App integration returned 403 on repo creation, not authorized for that). That file already contains a full App Store submission checklist template and the two portable iOS gotchas (Capacitor local-asset `status 0` fetch quirk, iOS Safari filtered-SVG repaint bug) pulled from this repo's memory. **Start a session in `highland-music-site` for portfolio/business-level questions; start a session in each app's own repo for that app's code.** Piano Chords Lab / Note Quest repos don't exist yet — once created, add them to the ops hub's "Apps" list and give each its own CLAUDE.md following this file's pattern.
- **Onboarding questionnaire + goal-based nudges shipped (2026-07-10):** User's idea, discussed and scoped together first (nudges-only, not automatic default overrides — see "Decided Against" if this comes up again as "just auto-apply the defaults"). Built `OnboardProfileSheet` (two optional tap-only questions: skill level + goal) shown once right before the first overview tour. Landed on `main` (commit 991a952, cherry-picked from feature branch `claude/onboarding-skill-questionnaire-ti24uc` commit 393e450, also pushed there). Full architecture in "What's Built" → "Onboarding questionnaire" entry. Four touch points, all dismissible/tap-to-apply, none automatic: tour copy (`tourStepsFor` gained a 3rd `profile` param + per-step `goalText`), Guide suggestion card (`GOAL_GUIDE_STAGE`), Play tab Drop 2 nudge, Ear Training Cadences nudge. Added 12 new smoke checks (Test 25 questionnaire flow + tour personalization + Guide nudge; Test 26 Play nudge applies Drop 2). Also fixed `freshPage()` in `test/smoke.cjs` to default `jg-onboard-seen='1'` so the new sheet doesn't block the 2 pre-existing tour-spotlight tests that intentionally leave the tour un-suppressed. `npm test` 44/44, `npm run test:smoke` 78/80 (2 known pre-existing Play-Essentials failures, unrelated to this change — documented baseline). Visually verified all 4 surfaces (questionnaire, tour goalText, Guide nudge, Play nudge, Ear Training nudge) via throwaway Playwright screenshots (deleted after use).
- **Play tab: tapping a bar now plays its chord (2026-07-04):** User reported clicking the lead-sheet bars (the chord-name cells above the neck) did nothing audibly. Root cause: the bar `onClick` only called `setActiveChordIdx`/`setEditingBar` — no audio was ever wired up for that tap, unlike `ChordBox`/Diatonic/Any-Chord taps which all use the module-level `playChordPreview` helper. Fix landed on `main` (commit 5f3075c, cherry-picked from feature branch `claude/play-tab-chord-audio-k0vg9h` commit 63cff18, also pushed there): added `playMidiPreview(midis)` next to `playChordPreview` in `app.js` — same tap-to-preview path (own `_getPreviewCtx`, independent of the Play tab's transport `audioCtxRef`) but takes raw MIDI notes directly instead of frets+strings, since the bar's sounding voicing is already precomputed per-bar in `compMidiRef.current` (used by `tick()` for playback). Bar click now plays `compMidiRef.current[barIdx]` (both chords in sequence, 450ms apart, for split bars); suppressed while `isPlaying` so it doesn't double up with the live transport audio. Verified via a throwaway Playwright script (deleted after use) that clicking a bar creates+starts real `AudioBufferSourceNode`s (3 notes, matching a shell voicing) — not just a visual state check. `npm test` 44/44, `npm run test:smoke` 67/69 (matches documented 2-failure baseline).
- **Ear training difficulty curve smoothed (2026-07-02):** User reported the interval-tier jumps felt too steep. Landed on `main` (commit 158da6f, cherry-picked from feature branch `claude/ear-training-levels-mgwhnr` commit a77ea42, also pushed there). `IVAL_TIERS` in `app.js` went from 3 tiers to 5: Lv1 Octave & Perfects (3 ivals) → Lv2 +Major 3rd/6th (5) → Lv3 +Minor 3rd/6th (7) → Lv4 +2nds/7ths (10) → Lv5 All 12 (tritone saved for last as classically hardest to place). Freemium boundary **unchanged** — Essentials still caps at 7 intervals (now tier 3 instead of tier 2), Pro still reaches all 12 (now tier 5 instead of tier 3); `maxTier=isEss?3:5` (was `isEss?2:3`). Updated `test/gating.test.cjs`'s exact-string assertion + matrix comment to match. Verified visually via a throwaway Playwright script (deleted after use): Essentials screenshot shows Lv1–3 unlocked + Lv4–5 🔒; Pro screenshot shows all 5 unlocked. `npm test` 44/44 and `npm run test:smoke` 67/69 (matches documented 2-failure baseline, both pre-existing Play-tab Essentials gating checks unrelated to this change).
- **Rating prompt shipped (2026-07-02):** Built + landed on `main` (commit f8d647e, cherry-picked from feature branch `claude/app-review-support-prompts-aisfjh` commit bfddba0, also pushed there). Adds `Rate` module + `RatePromptSheet` — asks "Enjoying Jazz Guitar Lab?" at streak day 14/60/180, Yes → native App Store review prompt, "Not really" → Contact Support link (avoids funneling unhappy users into public reviews). Full details in "What's Built" above. **Not yet live on device** — needs `npm run sync` to register `@capacitor-community/in-app-review` in the iOS project (added to `package.json` but Package.swift not synced, same state as local-notifications). `npm test` (44/44) and `npm run test:smoke` (67/68, matches documented baseline) both pass post-change.
- **Repo privacy decision (2026-07-02):** User asked whether to make `pablohoney1977/jazz-guitar-app` private before launch. Checked repo — no hardcoded secrets (RevenueCat/PostHog keys are still placeholders, Codemagic creds are in a secure variable group, not the repo). So not a security-leak issue, but recommended going private anyway before launch to avoid exposing unreleased jazz-standards content and the exact freemium paywall gating logic in `app.js`. User said yes. **Claude has no tool to change GitHub repo visibility** (github MCP here covers content/PRs/issues, not repo settings) — this is a manual user action: Settings → Danger Zone → Change visibility → Private. Not yet done as of this checkpoint.

- **LLC site BUILT + LIVE on GitHub Pages (2026-07-05):** LLC legal name is **Highland Music LLC** (no comma — per the WI Articles). Canonical domain = **`highlandmusicapps.com`** (bought at Porkbun; user also mentioned securing the `.net` — `.com` chosen as canonical, 301 `.net`→`.com` later if wanted). Site source is a **separate local repo** `C:\Users\pehre\Projects\highland-music-site` (NOT the Jazz Guitar App repo), pushed to public GitHub repo **`PabloHoney1977/highland-music-site`**. Static, no build step: `index.html` (Highland Music, LLC branding + all 3 apps: Jazz Guitar Lab, Piano Chords Lab, Note Quest), `support.html` (→ App Review Support URL), `privacy.html` (→ App Store Privacy Policy URL; covers PostHog + RevenueCat + local storage), shared `styles.css` (dark/light aware), `CNAME`=highlandmusicapps.com. LLC legal name in every footer + on-domain contact `support@highlandmusicapps.com` = the Apple LLC-association proof. **GitHub Pages enabled via REST API** (gh not installed; used cached git credential token per the github-no-gh-api-workflow memory). **DNS DONE + HTTPS LIVE (2026-07-05):** Porkbun DNS set (4 apex A records `185.199.108/109/110/111.153` + `www` CNAME→`pablohoney1977.github.io`; clicked Porkbun "Replace" over the parking apex ALIAS); all 3 URLs serve 200 over **https with Enforce HTTPS ON**, http 301→https. Two GitHub gotchas hit + fixed: first build stuck (`building` duration 0) → POST `/pages/builds` manual rebuild; HTTPS cert never auto-started → remove+re-add custom domain via `PUT /pages` (cname:"" then back) triggered Let's Encrypt → then `PUT {"https_enforced":true}`. **✅ The Individual→Organization conversion is DONE (2026-07-15) — this thread is closed; the site did its job.** Remaining nice-to-haves, no longer blocking: (1) Porkbun email forwarding `support@highlandmusicapps.com`→Gmail (MX already present); (2) set Porkbun registrant Organization = `Highland Music LLC`. ⚠️ **The site's footers say "Highland Music, LLC" — drop the comma** to match the WI Articles (see the legal-name note in "Legal / Business Structure"). Full detail in memory file highland-music-llc-site.md.
- **App icon redesigned + shipped to `main` (2026-08-22, commit bb195f6):** User rejected a fuller "refined dark" redesign — wanted to KEEP the original dark-fretboard styling, just modified. Final = original look (gradient bg #111124→#07070f, gold nut, thin frets, glowing dots, serif label) with: **6 strings** (⚠️ **string 1 = far RIGHT / high E**, so string index in code = `6 - stringNumber`), bottom label **JG → JGL**, and a new chord shape in the **4 tonal-center colors** (red/blue/gold/teal, not 3-blue+1-orange): **red s3/f1, blue s2/f2, gold s4/f2, teal s5/f3**. Assets updated: App Store PNG (`ios/App/App/Assets.xcassets/AppIcon.appiconset/AppIcon-512@2x.png`, square 1024), `icons/apple-touch-icon.png` (180), `icons/icon.svg` (favicon, hand-authored to match). **`www/` is gitignored** (build output — local copies updated but not committed; `npm run build` regenerates). Render script: scratchpad `icon_oldstyle.py` (PIL — no cairo on this Windows box, so SVG can't be rasterized locally; author SVG by hand + eyeball). Shows on device only after next Codemagic→TestFlight build; trivially changeable post-launch (ships in each build, normal review).
- **🔊 PLAY-TAB AUDIO ENGINE REBUILT (2026-08-16, commit fbe6537 on `main`; branch `claude/audio-quality-play-page-2y0ptp`).** User: "sounds synthesized and cheesy… not even close to good enough." Diagnosed the whole engine (hand-rolled Web Audio, no Tone.js) and fixed 7 things, in audible-impact order: (1) **ride was 100% synthetic filtered noise with only 2 buffers**, so the identical waveform played 8×/bar — replaced with **real FluidR3 GM cymbals** (ride1 + ride2 + bell, round-robin + per-hit pitch/level jitter); (2) **comping loaded only 3 guitar anchors** (F#2/F#3/F#4) → resampled up to **±6 semitones**; now loads all **10 already-bundled** samples, measured stretch **600c → 200c** on real playback (this also explains why chord previews sounded better than the backing track — the preview path always used the full map); (3) **bass was pinned to one octave** by `36+pitchClass` (B→C leapt *down* 11 semitones); now voice-led across E1–C3 via `bassMidiFor()`, with **G1/A#1 samples added** so the low end isn't stretched; (4) everything was **mono, centred, bone dry** → added procedural convolver room (generated IR, no asset) on a send + per-instrument stereo (bass centre, guitar L, drums R); (5) **perfect quantization at fixed 2:1 swing** → micro-timing + velocity jitter per instrument, tempo-dependent `swingFrac()`; (6) guitar EQ was scooping the instrument out (+7/−11/−17dB) → halved, plus velocity→brightness; (7) **no hi-hat** → pedal chick on 2 & 4 + feathered kick. **Levels were set deliberately conservative** (real samples peak near 1.0 vs the old shaped noise) — if the user says drums are too quiet, raise `base` in `playRideHit`.
- **Drum sample licensing (settled, don't re-litigate):** FluidR3 GM by Frank Wen is **MIT** ("I hereby release Fluid under the MIT license"); extracted from **`surikov/webaudiofontdata`**, which is also **MIT**. The **WebAudioFont *player* is GPL-3 and is deliberately NOT used** — GPL-3 is incompatible with App Store distribution. Notices ship in `samples/drums/LICENSE.md` and are credited in the About sheet. Samples dir is now ~2.9MB (drums ~198KB, +834KB for the two low bass notes).
- **In progress / mid-task:** _(audio rebuild live on `main`; **awaiting the user's ear** — they listen on device/web and report back. No further audio changes until then.)_
- **Bluetooth pedal (2026-06-30):** "Not working" report investigated. Web routing logic across all 5 tabs was already sound; root cause not reproducible from code alone (likely a pedal mode that emits **keyup-only** or unrecognized keys — real diagnosis needs the in-app tester on the device). Shipped low-risk hardening on `main` (commit 326b3cd): new `attachPedalListener()` listens for **both keydown + keyup** (deduped, ignores auto-repeat) so keyup-only pedal modes now fire; added `isEss` to the App handler deps. Pedal tester (About sheet → ⓘ) now catches keyup too and reports `eventType · code · keyCode` so an unrecognized pedal can be pinpointed. Added `pedalDir` unit test (was untested). **If still broken:** user opens About → Pedal tester, presses pedal, reports what shows — *nothing* = events not reaching page / wrong pedal mode (switch off Volume mode); *✗ + code/keyCode* = extend `pedalDir`; *✓* = bug is downstream.
- **Active branch:** landed on `main` (fix commit c50abff; self-test aca0949). Feature branch `claude/audio-quality-latest-build-4a2y3z` diverged from cherry-pick history — `main` is the source of truth.
- **✅ ROOT CAUSE (confirmed by on-device self-test, NOT a guess):** On iOS the sample files **DO fetch** but the `capacitor://localhost` scheme returns local assets with **HTTP `status 0` (`ok:false`)** instead of 200. Our 3 sample loaders bailed on `if(!r.ok)`, discarding valid audio → every path fell back to **Karplus-Strong** (tinny preview) or **silence** (Play-tab guitar). The self-test proved it: the 14 real files resolved with `status 0`; only the 2 intentionally-absent files (guitar E3/E4, midi 52/64) actually threw. This **refuted** all 3 prior hypotheses (audio session / decoder / bundling) — they were never the cause. Lesson: the device instrument paid off; don't ship blind audio fixes.
- **THE FIX (c50abff):** in all 3 loaders (`_loadGuitar` + Play-tab bass/guitar prefetch) accept status 0 — `if(!r.ok && r.status!==0) return null;` then read `arrayBuffer()` and guard on `byteLength` (so a real 404/missing file still falls through). Web/PWA behaviour unchanged (real 200s pass, real 404s rejected). Self-test upgraded to read the body regardless of `r.ok` and verdict now keys on bytes-served; **expected next-build verdict: "FIXED ✓ — 14 served and decoded (iOS status-0 quirk)".**
- **Self-test tool (keep — useful for future audio bugs):** hidden, read-only, **long-press (700ms) the header title "Jazz Guitar Lab."** `runAudioSelfTest()` + `AudioDiagSheet` near line 644. Reports per-file status/bytes/decode, ctx state, A/B sampled-vs-KS, Copy-report, one-line verdict.
- **Next concrete step:** user listens to the Play tab (live on `main` now, no build needed for web/PWA) and reports back. Then: TestFlight build to confirm on device, incl. long-press the title → Run self-test → expect "FIXED ✓ — 23 served and decoded" (was 14; drums + 2 bass notes added).
- **Open questions for the user:** What was the last build number? (asked; user said maybe 28 — unconfirmed, I can't read Codemagic/ASC from here.)
- **✅ EIN RECEIVED (2026-08-16, user reported).** Unblocks the whole payout chain: open free LLC checking (Mercury/Relay/Bluevine) → App Store Connect banking + W-9 under the LLC/EIN → Paid Applications Agreement goes Active → RevenueCat payout to the LLC. Watch-item still live: Apple's account legal name has a comma ("Highland Music, LLC") vs the EIN/Articles name **`Highland Music LLC`** (no comma) — likely the culprit if a name-match check complains.

## Big Picture Goal
Ship Jazz Guitar Lab as a **freemium iOS App Store app** targeting adult guitarists who want to learn jazz harmony. Monetization: **free download** (Essentials tier) with a **$14.99 one-time IAP** to unlock Pro. No subscription.

**Freemium split:**
- **Free (Essentials):** Shell voicings only, major II-V-I only, melodic intervals (5 consonant intervals), first 4 chord types in Any Chord tab, all 16 Guide stages (no feature gating in Guide)
- **Paid Pro ($14.99):** Drop 2/3/Rootless voicings, minor II-V-I + jazz blues + I-VI-ii-V turnaround + tritone sub + sec dom + custom + 5 jazz standards (Blue Bossa, Autumn Leaves, All The Things You Are, Stella by Starlight, There Will Never Be Another You), all 12 intervals + harmonic mode + triads + 7th chords + cadences + Auto ear training in Ear Training, all extended chord types (9ths, 11ths, 13ths, altered) in Any Chord, Favorites (save/restore progressions) in Play

**Destination:** Apple App Store via Capacitor (web→native wrapper) + Codemagic (cloud CI/CD build — no Mac required).
**Timeline:** Ship as soon as the product is ready.
**Apple Developer account:** Not yet set up ($99/year enrollment needed). Also enroll in the Apple Small Business Program (15% cut instead of 30% — raises net per sale from $10.49 to $12.74 on a $14.99 purchase).
**Bundle ID:** `com.pablohoney.jazzguitarlab`
**Support:** Simple email channel (no dedicated support infrastructure needed pre-launch).
**Marketing:** Organic only — ASO (App Store search optimization), YouTube guitar community, Reddit (r/jazzguitar, r/guitarlessons), musician forums. No paid ads — one-time IAP economics don't support paid acquisition (CPI would exceed LTV). Build audience through content and community.

## Stack
Single-file React 18 PWA. No build step. CDN React, all inline styles, ~5100 lines in `app.js`. Serve from `main` branch, develop on feature branches. Target: iPad (720px content width) primary, iPhone secondary.

Capacitor iOS project lives in `ios/`. Build script (`npm run build`) copies web assets to `www/` before `cap sync`. Codemagic config is in `codemagic.yaml`.

## App Store Deployment Checklist
Steps completed and still needed to ship:
- [x] Capacitor iOS project initialized (`ios/` directory committed)
- [x] `codemagic.yaml` build pipeline configured
- [x] Bundle ID decided: `com.pablohoney.jazzguitarlab`
- [x] Enroll in Apple Developer Program — developer.apple.com, $99/year. **✅ Converted to Organization 2026-07-15 — Highland Music LLC is now the seller.** Liability-shield goal achieved on the Apple side.
- [x] **App Store Connect banking + W-9 under the LLC's EIN — DONE 2026-08-24.** Capital One LLC checking (…3825, USD) + W-9 (submitted 2026-08-22) + **Paid Apps Agreement Active** all confirmed on the Business page. Whole `EIN → bank → W-9 → Paid Apps Active` chain is complete; Apple pays the LLC directly. See "Legal / Business Structure".
- [ ] Sign up for Codemagic — codemagic.io, connect to GitHub repo
- [ ] Register bundle ID in Apple Developer portal
- [ ] Create App Store Connect API key, paste into Codemagic dashboard
- [ ] Create App Store listing (name, screenshots, description, pricing)
- [x] Set up $14.99 one-time IAP in App Store Connect (product ID: `pro_unlock`) — **DONE 2026-07-16.** IAP created (Non-Consumable, "Prepare for Submission"). **RevenueCat fully wired:** app connected, entitlement `pro`, `pro_unlock` attached, default offering → Lifetime package → `pro_unlock`, `appl_` public key live in app.js (commit 7ee6460). Ready for sandbox test after `npm run sync` + build. Remaining before submission: confirm price = $14.99 tier + add IAP review screenshot. Real sales still gated by EIN→banking→Paid Apps Agreement Active.
- [ ] Add Capacitor Local Notifications plugin (for practice streak reminders — defer until after first build succeeds)
- [x] **`npm run sync` / plugin registration — NOT a gate after all (corrected 2026-08-16).** Codemagic runs `npx cap sync ios` on every build, so plugins are registered in CI regardless of the committed Package.swift. Verified by running the sync (`Found 3 Capacitor plugins for ios`) and committing the result (ddcf82e). See the Working Memory entry.
- [ ] **Sandbox IAP purchase test on device** — the part that *is* still unverified. Needs a TestFlight build + a human; no EIN required for sandbox.
- [ ] App icon: needs all required sizes (currently have `icon.svg` and `icons/` — need to verify App Store required sizes)
- [ ] Splash screen: review default Capacitor splash, customize if needed
- [ ] First TestFlight build via Codemagic
- [ ] Internal testing
- [ ] **DSA trader address fix (before EU submission)** — Apple's Digital Services Act step was completed 2026-07-16 with the **home address** (531 24th St N) because the field wouldn't edit inline; trader address+phone+email display **publicly** on the EU App Store page. Not yet exposed (only goes public once an app is EU-live). Before launch, change address → **708 Water St, Genoa, WI** (the motel — real commercial mail-receiving address), phone → a Google Voice number, email → support@highlandmusicapps.com. Edit path likely in membership/legal-entity details, not the trader dialog; Apple Dev Support handles DSA corrections if locked. See memory `dsa-trader-address.md`.
- [ ] **iPad screenshots — 13" (2064×2752).** Hard-blocks submission; iPad is the primary target so iPad support is declared. iPhone 6.9" (1320×2868) also required.
- [ ] **Set up `support@highlandmusicapps.com`** (Porkbun forwarding → Gmail). Never done; it's the address App Review and real users write to.
- [x] **Enroll in the Small Business Program** (15%) — **submitted 2026-08-24, awaiting approval.** Effective the 1st of the month *after* approval, never backdated. **Not a launch blocker** — the rate depends on approval date, not launch date, so waiting for it buys nothing (see Working Memory). Remaining: confirm the enrollment submitted rather than sitting as a draft.
- [ ] **Add `ITSAppUsesNonExemptEncryption=false` to Info.plist** — HTTPS-only is exempt; stops the per-build export-compliance question.
- [ ] **Select "Manually release this version" at submission** — the default ships the app the moment it's approved, wasting the coordinated launch day. This is what makes the Sat-release/Sun-burst split possible.
- [ ] **Sat 8/29: Release, then verify before anyone is watching** — hit Release Saturday morning (propagation takes ~1–4h), then: search for it in the App Store, **install on a device that has never had the app**, **buy `pro_unlock` with a real card** (sandbox ≠ production — different servers, receipts, failure modes), tap both links from a phone, put the App Store link on highlandmusicapps.com. **Send nothing Saturday.**
- [ ] **Sun 8/30 morning US: fire the friend burst in one window** — ranking counts the day downloads land, not the release date, so the Saturday release costs nothing on the charts.
- [ ] App Store submission

## Legal / Business Structure
> NOT legal advice — confirm specifics with a small-business/IP attorney or CPA. Captured here so future sessions don't accidentally cross a line.

**⚠️ EXACT LEGAL NAME: `Highland Music LLC` — NO COMMA.** The WI Articles of Organization read exactly that. Anywhere below (or in the `highland-music-site` footer) that says "Highland Music, LLC" with a comma is **wrong for legal/tax/banking records** and should be corrected — the comma is fine in marketing prose, but EIN/W-9/bank/Apple records must match the state filing character-for-character. WI **Entity ID `H079857`** (state ID, *not* an EIN), filed **12/8/2024**, single-member → disregarded entity.

**EIN — applied for, PENDING (as of 2026-07-15):**
- SS-4 **faxed 2026-07-14** to **855-641-6935** (50-states number, verified against irs.gov). **CP 575 arriving BY MAIL, ~4 weeks (~mid-Aug 2026)** to 531 24th St N — the return-fax box was left blank deliberately (free send-only fax; chose free-and-slower over a paid inbound number).
- **Why not online:** the IRS online EIN assistant hard-fails with **reference 101** (name-conflict flag). LLC filed 12/2024, so it's not a state-sync timing issue and won't self-clear. **The phone is a dead loop** — 1-800-829-4933 tells domestic callers "online only" and points back at the tool that 101'd; IRS discontinued domestic phone EIN issuance. **Fax is the only working path** (a human can clear a 101). Don't re-suggest the online tool or the phone.
- **When it arrives: save + back up the CP 575 PDF** — authoritative record; reissue otherwise needs a 147C letter.

**Bank account (blocked on EIN):**
- **Open a dedicated LLC business checking account** — free options: **Mercury / Relay / Bluevine** ($0/mo, no minimum). **E\*TRADE has no real business checking** (brokerage only) — wrong tool for payouts. Requires the **EIN**, not the SSN.
- **Decision 2026-07-15: do NOT bridge via personal banking.** Pre-launch = no revenue flows, so it buys nothing; and now that the **seller is the LLC**, personal bank + personal-SSN W-9 is an **entity mismatch** risking an Apple tax-info review / payment hold. Only reconsider if the **Paid Applications Agreement** (needs banking + tax info) hard-gates the `pro_unlock` IAP on the critical path.
- Fund via a documented **owner capital contribution**; pay yourself via **owner's draws**. **Commingling is the #1 veil-piercing fact** — the shield only holds if the LLC is genuinely separate.

**Entity / liability:**
- The LLC only shields personal assets if the **LLC is the actual seller/operator** of the app. An Apple **Individual** enrollment publishes under the owner's personal name → no entity shield for app-related claims.
- **✅ DONE 2026-07-15: the Apple Developer account is converted to Organization — Highland Music LLC is the seller.** The website/domain-association blocker is cleared. (Historical detail on how the conversion was pursued follows.) It was an in-place conversion — *no new account, no second $99*. Org type ≠ "big company"; a single-member LLC qualifies.
  - Steps: (1) get a **D-U-N-S number** for the LLC first (free, ~1–2 weeks, gating item); (2) Apple Developer site → **Contact us → Membership and Account → Program Enrollment**; (3) request "convert Individual → Organization" with LLC legal name, signer, D-U-N-S, legal address/phone; (4) Apple verifies + emails a form to finish.
  - **Do this BEFORE first submission** — converting before there's a live app + live IAP avoids the messier app-transfer path (app transfers have IAP restrictions and aren't easily reversible).
  - **Confirmed hard requirement (2026-07-04, verified via Apple's official enrollment docs):** Organization enrollment also requires (a) a **public, functional website whose domain is clearly associated with the LLC** — a bare landing page, placeholder, or social-media-only link is explicitly rejected — and (b) a **work email on that same domain** (not Gmail), in addition to the D-U-N-S number. Plan: buy a domain matching the LLC/app name, put up a minimal real site on it (can double as the support page App Review requires anyway), get domain email via a free forwarder or Zoho, THEN pursue D-U-N-S + the Individual→Organization conversion.
  - **Domain SECURED (2026-07-05): `highlandmusicapps.com`** at Porkbun ($10.08/yr). WHOIS privacy is ON by default (RDAP-confirmed redacted — does not publicly show the LLC yet). To satisfy Apple's LLC-association check: set Porkbun registrant Org field = LLC legal name; primary path = **LLC legal name in site footer/contact + domain email** (keeps privacy ON); fallback = toggle Porkbun WHOIS privacy OFF during review only if Apple asks. Domain email: use free Porkbun forwarding→Gmail or a free Zoho mailbox (NOT the $36 Porkbun hosting). Site: free GitHub Pages on a new **public** repo. Still needed: build the site, set up domain email, get D-U-N-S (~1–2 wk gate), then request the Apple Individual→Organization conversion.
- **Small Business Program (15% rate) is independent of account type** — both Individual and Org accounts qualify (under $1M/yr). It is NOT the moment you switch entity type; enroll in it separately, anytime.
- Maintaining the shield is on the user: **separate LLC bank account, no commingling**, LLC owns the app/IP (and ideally the RevenueCat account + domain). An LLC does not shield against one's *own* personal wrongful acts (e.g. knowing infringement).

**Music licensing — jazz standards on the Play tab (chords-only boundary):**
- A song's copyright protects **melody + lyrics**, NOT **chord progressions** (progressions are treated as uncopyrightable functional building blocks). Play tab uses **only the chord changes + its own generated backing audio** — no melody, no lyrics, no copyrighted sound recording. That combination is the low-risk zone.
- The standards are **NOT public domain** (e.g. Autumn Leaves / Kosma 1945 is under copyright until ~2040). Safety comes from *what is used* (chords + title-as-label), not from age. Song **titles aren't copyrightable**, so naming a progression "Autumn Leaves" is fine.
- **DO NOT cross the line:** no melody playback, no notated lead sheets/transcriptions, no lyrics for copyrighted tunes (Autumn Leaves, Blue Bossa, Stella by Starlight, All The Things You Are, There Will Never Be Another You) without licensing + counsel. Adding any of those flips this from chords-only into needing a license.

## What's Built (current `app.js` features)
- **5 nav tabs:** Guide, Chords (Diatonic), Any Chord (Custom), Play (II-V-I), Ear Training
- **Freemium paywall:** `UpgradeSheet` bottom sheet triggered by 🔒 lock badges on gated features. `showUpgrade(feature)` / `doUpgrade()` in App. **IAP wired via RevenueCat** (`IAP` module near top of `app.js`, mirrors the `Notif` no-op-on-web pattern): `doUpgrade`/`doRestore` run a real `purchasePackage`/`restorePurchases` on a native build once `__REVENUECAT_IOS_KEY__` is filled in; otherwise (web/PWA, or native pre-key) they fall back to the dev-unlock `setLevel('pro')`. A launch effect re-grants Pro from the receipt. Entitlement id `pro`, product `pro_unlock`. **Header tier chip (`tierChipStyle`)** is tier-aware: Essentials → gold `✦ Pro` pill that opens the paywall (generic `showUpgrade('Pro overview')` — the only non-lock discovery path); trial → `Trial ✦` (tap previews Essentials); real purchased Pro → on a native build (`IAP.isNative()`) an **inert** `Pro ✦` status badge (no downgrade footgun), on web/PWA a dev-only revert for testing. (The old "tap any Pro chip to revert" was a dev leftover — removed for real-Pro on device.)
- **7-day Pro trial ("taste of Pro"):** `UpgradeSheet` shows a "Try Pro free for 7 days" secondary button, only when no trial started yet. Stored as `jg-trial-start` (date string). `trialActive` is computed from that date (<7 days). KEY ARCHITECTURE: `effectiveLevel = (level==='essentials' && trialActive) ? 'pro' : level` is what gets passed to ALL four child views (Guide, Play, Keys/CustomChord, Train) and drives `isEss`. The purchased `level`/`jg-level` is NEVER set to 'pro' during a trial — only `effectiveLevel` lifts gates, so an expired trial cleanly reverts without a stale 'pro' confusing future IAP wiring. Header shows "Trial ✦" (vs "Pro ✦"); tap toggles `trialActive` off to preview Essentials. When trial expired, `UpgradeSheet` shows "Your free trial has ended" copy. `trial.started` tracked via PostHog.
- **First-time onboarding:** Brand-new users (no `jg-viewMode` saved, no `jg-path` progress) land on the Guide tab. Returning users go straight to their last view. Logic in `viewMode` useState init.
- **Onboarding questionnaire (2026-07-10):** `OnboardProfileSheet` — two optional tap-only questions (skill level: Beginner/Intermediate/Advanced; goal: Comping & voicings/Ear training/Improvisation/Just exploring) shown once, right before the first overview tour (gated alongside `jg-toured` via `jg-onboard-seen`; profile persisted to `jg-onboard-profile`). Purely additive — never auto-changes gating or persisted defaults. Drives four dismissible, tap-to-apply nudges: (1) tour steps get a goal-specific sentence appended (`tourStepsFor(steps, isPro, profile)` — third param) via each step's optional `goalText` map; (2) Guide shows a dismissible card suggesting a starting stage per `GOAL_GUIDE_STAGE` (only for intermediate/advanced — beginners keep the default in-order start); (3) Play tab offers to switch to Drop 2 voicings for comping/improv goals (Pro/trial: `setVType('drop2')`; Essentials: opens `UpgradeSheet` naming "Drop 2 voicings"); (4) Ear Training offers to jump to Cadences for the ear-training goal, same Pro/trial-vs-Essentials split. Each nudge has its own one-time dismiss flag (`jg-guide-goal-nudge`/`jg-play-goal-nudge`/`jg-ear-goal-nudge`).
- **Guide tab:** 16 ordered learning stages with expandable content, tappable checklist items (persisted to `jg-path-items`), resume card, phase labels, links to live presets. Collapsible "Start Here" intro (open for new users, collapsed once there's progress; choice persisted to `jg-guide-intro`). **Scroll-restore on return:** GuideView unmounts on tab switch, so a module-level `guideReturn` snapshots scroll position + open stages on unmount and restores them on remount within the session (fresh reload still jumps to current stage). Scroll is captured via a scroll listener, not `window.scrollY` at unmount, because nav-away calls `window.scrollTo(0,0)` synchronously first.
- **Chords tab:** All 7 diatonic chords in any key, shell/drop2/drop3/rootless voicings, scale overlay, guide tones, fingering numbers
- **Any Chord tab:** All chord types including extensions, find-in-key, custom root picker. Two modes: **Build a Chord** (pick root + type, see voicings on neck) and **Find Chord** (Pro — tap notes on fretboard to identify chord). Find Chord: exact matches show chord name + "open ↗" to jump to Build a Chord; incomplete matches are clickable and highlight missing chord tones as blue dashed dots on the fretboard so you can tap them to complete the voicing.
- **Play tab (IIVIView):** Backing track with walking bass, ride cymbal, jazz guitar comping. Forms: major/minor II-V-I, jazz blues, I-VI-ii-V turnaround, tritone sub, sec. dom., custom. Standards (Pro): Blue Bossa, Autumn Leaves, All The Things You Are, Stella by Starlight, There Will Never Be Another You. Swing feel, variable BPM (35–150). Voice leading, pinned chords, bar-level voicing override. Per-instrument **MIX controls** (Bass/Guitar/Ride): each has a LED enable toggle + MIX button that opens a 5-band EQ + volume slider (keys `jg-eq`/`jg-geq`/`jg-req`, plus volume state). **Favorites (Pro):** ★ saves current form+BPM+voicing (+custom prog / bar voicings); saved chips restore in one tap (`jg-faves`). In Essentials, Favorites shows as a locked dashed "🔒 Pro" discovery card that opens the UpgradeSheet. Both MIX (`play-mix`) and Favorites (`play-faves`) have contextual tour steps.
- **Ear Training tab:** Interval recognition (melodic + harmonic), triads, 7th chords, cadence recognition (II-V, V-I, II-V-I, I-VI, iv-I). Essentials: consonant intervals (melodic) only, single "3 more modes 🔒" upgrade CTA (not individual per-tab badges). Pro: all 12 intervals + harmonic mode + triads + 7th chords + cadences. Nav row uses ← ♪ → circle buttons (always in viewport, no scroll needed).
- **App Store rating prompt:** `Rate` module (mirrors `Notif`/`IAP` no-op-on-web pattern) wraps `@capacitor-community/in-app-review`'s native `requestReview()` (SKStoreReviewController on iOS). `RatePromptSheet` fires at streak day 14/60/180 ("Enjoying Jazz Guitar Lab?"), delayed 5.6s past the streak-milestone card's dismiss so they don't stack. Yes → native review prompt, locks out future asks (`jg-rate-asked`); "Not really" → opens `SUPPORT_URL` (Contact Support) instead of risking a public bad review, 60-day cooldown before re-asking (`jg-rate-last-ask`). Tracked: `rating.prompt.shown/yes/no`. **Needs `npm run sync` (cap sync ios)** to register the plugin natively before it does anything on device — same not-yet-synced state as `@capacitor/local-notifications`.
- **Two-tier tour system:** App overview tour (5 steps across nav tabs) + per-page contextual tour for each tab. **Tier-aware:** `tourStepsFor(steps, isPro, profile)` adapts copy for Pro users — drops the pricing-pitch step (`essentialsOnly:true`) and swaps "unlocks with Pro" wording for `proText`/`proTitle` describing features they already have — and (third param, optional) appends a goal-specific sentence from the onboarding profile via each step's `goalText` map. Used in both render and the `overviewNext`/`pageTourNext` length checks.
- **Streak tracking:** 🔥 Xd badge in header. Fires when Play tab session starts OR first Ear Training answer. Resets if day is skipped. `playSessions` counted in localStorage. Push notification reminders deferred to Capacitor build.
- **Streak milestones:** Celebration card slides up at days 3, 7, 14, 30. Auto-dismisses at 5.4s. Tap to dismiss early. `streakMilestone` state, `STREAK_MILESTONES=[3,7,14,30]`, `milestoneUp` CSS animation in index.html. Days 7 and 30 show an inline upgrade nudge for essentials users.
- **CRO upgrade CTAs:** Guide Stage 16 ("standard") shows a gold upgrade card before "I've got this" (essentials only). Guide allDone graduation card has an Unlock Pro button (essentials only).
- **Analytics:** PostHog CDN snippet in `index.html` with `__POSTHOG_KEY__` placeholder (no-ops until key is set — user must replace after signing up at posthog.com). `track(event, props)` helper in `app.js`. Events tracked: `app.loaded`, `paywall.shown {feature}`, `upgrade.completed {feature}`, `trial.started`, `guide.stage.completed {stage_id}`, `streak.milestone {days, level}`.
- **Dark/light theme toggle**
- **Bluetooth page-turner pedal support** (AirTurn / PageFlip keyboard events)
- **PWA:** `manifest.json` + `sw.js` service worker for offline caching

## Test Suite
- **Unit + gating:** `npm test` — 44 checks (theory, content, freemium gate assertions). Lives in `test/theory.test.cjs`, `test/content.test.cjs`, `test/gating.test.cjs`.
- **Smoke (Playwright):** `npm run test:smoke` — 96 checks across 27 test blocks. Headless Chromium, iPhone 14 viewport (390×844). Covers layout, interactive flows, upgrade sheet, BPM control. Screenshots written to `test/screenshots/`.
  - **React UMD copies required (fresh-container gotcha):** CDN is blocked (note: it's **cdnjs.cloudflare.com**, not unpkg), so the test maps the React CDN URL to local `test/react.production.min.js` + `test/react-dom.production.min.js`. These are gitignored and **missing in every fresh container** — without them the app never mounts and you get ~22 failures all stemming from `React is not defined`. This is environmental, not a regression. Recovery: `npm install react@18.2.0 react-dom@18.2.0 --no-save --prefix /tmp/react-umd` then copy both `*/umd/react*.production.min.js` into `test/`. Trust `npm test` (unit/gating) there.
  - **✅ The suite is fully green (85/85) as of 2026-07-26 — there is no longer a "known failures" baseline.** The 2 long-standing Play-Essentials failures were **stale assertions**, not app bugs: they encoded a retired "one consolidated CTA" design, while the app deliberately shows the 11-item locked-forms horizontal-scroll shelf. Test 23 was rewritten to assert the shelf (1 selectable form, ≥8 locked chips, strip scrolls without page overflow, a locked chip opens the paywall and does *not* switch the form). **If you see a red check now, you broke something — don't wave it through as "the known baseline."**
- **Audio engine:** `npm run test:audio` — 14 checks, `test/audio-engine.cjs`. Starts a real playback run in headless Chromium and instruments the **live audio graph**: fingerprints each sample file by content hash (⚠️ all 10 guitar mp3s are byte-identical in length, so a byteLength key silently collapses them — use the hash), tags every decoded `AudioBuffer`, then records every `AudioBufferSourceNode` started. Asserts which buffers play, pitch-shift distance (incl. a direct new-vs-old sample-map comparison on the same notes), that the bass escapes the old C2–B2 window, and that the convolver + 3 panners exist. This is the only suite that can catch audio regressions — the smoke suite only proves the UI renders.
- **Exploratory harness:** `npm run test:explore` — multi-persona simulation. 4 parallel sessions, each 20 steps, seeded PRNG for reproducibility (`--seed N`). Generates structured report in `test/explore/reports/YYYYMMDD-HHMM/`. Set `ANTHROPIC_API_KEY` for LLM synthesis of findings. Reports dir is gitignored.

## Audio Architecture
> Rebuilt 2026-08-16 (commit fbe6537) — see the Working Memory entry for the full before/after. Engine is hand-rolled Web Audio (no Tone.js), scheduled by `tick()` on a 25ms lookahead.
- **Play-tab signal path:** note → instrument filters → gain → **per-instrument bus** (`busRef.current.{bass,guitar,drums}`, each a `StereoPanner` + reverb send) → `DynamicsCompressor` → destination. Reverb is a shared `ConvolverNode` fed by `makeRoomIR()` (procedural IR, no asset). Panner creation is feature-guarded — older WebKit just stays centred.
- **Drums are real samples** under `./samples/drums` (FluidR3 GM, MIT — see `samples/drums/LICENSE.md`): `ride1`/`ride2`/`ridebell` (round-robin, bell on 18% of beat-1 accents), `hihat-pedal` (chick on 2 & 4), `hihat-closed`, `sidestick`, `kick` (feathered, 0.07 gain). `playDrum()` + `playRideHit()`. The old synthetic `makeRideBufAsync` chain is **kept only as a fallback** if the samples fail to load — don't delete it.
- **Humanization:** `jitterSec(ms)` / `jitterVol(v,amt)` / `swingFrac(bpm)` at module level. Comping lays ~6ms behind the beat, ride gets the most timing licence (±7ms), bass the least (±2.5ms) since it keeps time. Swing ratio is tempo-dependent (0.68 at ≤80bpm → 0.56 at ≥200bpm), **not** a fixed `2/3` — don't reintroduce hardcoded triplet offsets.
- **`bassMidiFor(pc,prev)`** picks the bass register by voice leading within `BASS_LO..BASS_HI` (29–48) with a pull toward `BASS_CENTER` (38). Do NOT go back to `36+pitchClass` — that pinned every note into C2–B2 and made the line leap instead of walk.
- Guitar/bass samples **bundled locally** under `./samples` (`GUITAR_SAMPLES` + the bass/guitar `BASE` consts use relative paths so they resolve on the Pages-subpath PWA *and* the Capacitor native bundle, offline). Pre-fetched as ArrayBuffers on load. Sourced from `nbrosowsky/tonejs-instruments`; build copies `samples/` → `www/`, sw.js best-effort precaches them. E3/E4 guitar samples (midi 52/64) 404 upstream and are intentionally absent — loader pitch-shifts from neighbors.
- `playGuitarNote(ctx, midi, startTime, sustainSecs, vol)` — biquad EQ chain for jazz tone (warmth boost 180Hz, presence cut 2200Hz, hi-shelf cut 3500Hz)
- `playGuitarChord(ctx, midiNotes, startTime, sustainSecs, vol, strum)` — stagger per string, volume taper
- `playChordPreview(voicing, strings)` — module-level tap-to-preview helper (own `_getPreviewCtx`, independent of the Play tab's `audioCtxRef`). Used by `ChordBox` taps, the Diatonic chord cards, and the Build a Chord Root/Type/Extension selectors (via `previewSelection` in `CustomChordView`). Selector handlers compute tones from the passed args, NOT state — `setState` is async so derived `tones` lag a render. Always previews a shell voicing for consistency.
- `pickStrum(isStab)` — probabilistic strum direction/speed
- Bass: Web Audio API synthesis with `bLowBoost`, `bThump`, `bMidCut`, `bHiCut` filter chain
- Ride cymbal: pre-rendered via `OfflineAudioContext`
- Beat scheduling: `tick()` with `nextTimeRef`, `beatRef`, `barPatternRef`

## Key Architecture Notes
- `app.js` is the only source file — all components, data, audio, styles in one place
- CSS variables in `index.html` for dark/light theme (two blocks: `:root` dark, `:root[data-theme="light"]` light). Theme set via `document.documentElement.dataset.theme`.
- Tonal center colors: `TC = ['#FF6B6B','#4ECDC4','#74C0FC','#FFD43B']` (Root, 3rd, 5th, 7th) — teal restricted to chord-tone contexts only
- **Accent colours as text (enforced by smoke Test 27):** never put a bright accent hex in a `color:`. Use `TCT[i]` / `TCT_RL[i]` for chord tones, `A_RED`/`A_TEAL`/`A_BLUE`/`A_AMBER`/`A_GREEN`/`A_PURPLE`/`A_ORANGE`/`A_WARN` for standalone accents, `GOLD_TXT` for gold text (`GOLD` stays the CTA-background/border value), and `accTxt(x)` where one variable feeds both a border and a label (`modeBtn`'s `col`, `FORM_DEFS[].col`, the diatonic `qcol`). Dots, glows, fills and borders keep the vivid hex — the split is the whole point.
- **Light-mode contrast:** bright pastels (the TC colors, instrument LED hexes `#74C0FC`/`#86EFAC`/`#FFD43B`) are legible on the dark bg but wash out as *text* on the light bg. Pattern: keep the bright hex for dots/glyphs/glows, but route label/text color through a theme-aware CSS var that darkens in light mode (e.g. `--led-bass-fg`/`--led-guitar-fg`/`--led-ride-fg` for the Play tab BASS/GUITAR/RIDE + MIX labels; `--dot-lbl` for dark-on-pastel chord-tone dots). Add new such vars to **both** `:root` blocks.
- `e()` = `React.createElement` alias used throughout
- `localStorage` keys: `jg-path` (guide done), `jg-path-items` (per-stage checklist), `jg-guide-intro` (Start Here collapsed state), `jg-chord-picker` (Any Chord root/type disclosure, `'1'`=open, default collapsed), `jg-streak`, `jg-last-practice`, `jg-play-sessions`, `jg-level`, `jg-trial-start` (7-day Pro trial start date), `jg-key`, `jg-bpm`, `jg-form`, `jg-toured`, `jg-eq`/`jg-geq`/`jg-req` (bass/guitar/ride EQ), `jg-faves` (Play favorites), etc.
- `SCALE_HINTS` has dom7 with 4 options including Phrygian Dom
- Unplayable voicings (Chords + Any Chord tabs): `calcVoicing` returns null when a drop voicing needs a wider-than-hand stretch. Handled in 3 layers — (1) a `playableSets` memo flags which string sets yield a shape for the current chord; (2) an effect auto-snaps `ssIdx` away from a dead set to `firstPlayableSet`; (3) dead string-set buttons are disabled/grayed (not removed — keeps layout stable), and when `noDropShape` (no set works at all) the neck is hidden and the full-width `NoShapes` notice shows. Shells/rootless already auto-jump via `firstValidShell`/`firstValidRl`. Don't "fix" the empty fretboard by removing options — disable, don't delete.
- **Fretboard display invariant:** `NeckSVG` only draws 15 frets (`NF=15`), so `calcVoicing` must never return a shape whose max fret exceeds 15 — anything higher renders as a dot floating off the right edge of the neck. When a computed voicing lands above fret 15, `calcVoicing` drops it an octave (even if that introduces an open string or a low-position stretch `spanOK` would otherwise reject) to keep all dots on the board.
- Essentials tier: shell voicings only, major II-V-I only, melodic intervals (consonant) only in ear training, first 4 chord types in Any Chord; gated features show 🔒 badges that trigger UpgradeSheet
- Pro tier: all voicings, all play forms, all ear training modes, all chord types
- **iOS Safari SVG repaint gotcha:** SVG elements that use a `filter` (e.g. the `url(#ng)` blur/glow on NeckSVG highlight dots) do NOT visually repaint on iOS Safari/WebKit when React updates their attributes — they stay stale until a user gesture forces a reflow. This caused the "Keys page chords don't update until I tap them" bug. Fix is to force a GPU compositing layer on the affected `<svg>` via `style:{transform:'translateZ(0)',WebkitTransform:'translateZ(0)'}`. Chromium does not reproduce this — only real iOS Safari does, so it can't be caught by the headless Playwright smoke tests. Apply the same `translateZ(0)` hint to any new filtered SVG that re-renders on state change.
- Voicing selection (`ssIdx`/`invIdx`/`shellIdx`) is **shared App state** between the Keys (diatonic) and Any Chord (`CustomChordView`) views, so the chosen string set/inversion/shape survives the "In a key ↗" / "Explore ↗" bridges and plain tab switches. `CustomChordView`'s reset-on-chord-change effects are skip-on-mount guarded (`typeChangeMount`/`shellResetMount` refs) so a carried-in voicing isn't wiped. Extensions (9/11/13) live **only** in Any Chord — the Keys map is the seven diatonic 7th chords, so jumping to a key shows the underlying 7th in the same voicing geometry.

## Decided Against (don't re-suggest)
- Comping rhythm patterns
- Chord melody examples
- Enclosures
- Guitar tone unlocks / EQ presets labeled as guitar models (sounds like EQ, not like actual guitars)
- Locking Pro version behind achievements (advanced users hit a wall = bad reviews)
- Subscription pricing (one-time $14.99 is the model)

## Pending / Next Session Priorities
1. **PostHog key** — user action: sign up at posthog.com, create project, replace `__POSTHOG_KEY__` in `index.html` with the real API key. Once live, paywall funnel (paywall.shown → upgrade.completed) will be visible.
2. **IAP implementation** — DONE in code (RevenueCat, `IAP` module in `app.js`, `@revenuecat/purchases-capacitor` dep). Remaining is config, not code: (a) enroll in Apple Developer, (b) create the `pro_unlock` non-consumable in App Store Connect, (c) create a RevenueCat project with entitlement `pro` + default offering, (d) replace `__REVENUECAT_IOS_KEY__` in `app.js` with the public iOS SDK key (`appl_…`). NOTE: the 7-day trial is honor-system / client-side only (`jg-trial-start` in localStorage). Fine for launch; if abused, gate via StoreKit intro-offer.
3. **App Store assets** — app icon in all required sizes, screenshots, store description copywriting
4. **Apple Developer enrollment** — user action required, $99, developer.apple.com. Also enroll in Small Business Program (15% cut → ~$12.74 net per sale).
5. **Code audit** — re-renders, audio memory leaks, edge cases in `calcVoicing`/`calcFingering`
6. **Add Capacitor Local Notifications** — practice streak reminders (defer until after first TestFlight build)
